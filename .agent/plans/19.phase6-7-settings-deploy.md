# Phase 6 + 7: Settings, Polish, Deploy, Cleanup

**Prerequisite:** Read `.agent/plans/15.convex-migration-progress.md` first for context.
**Depends on:** Phase 5 complete (chat works end-to-end)

**Complexity:** ✅ Simple (can be done in one session)

## Phase 6: Settings + Polish

### 1. Implement settings Convex functions
- `convex/settings/queries.ts` — getSettings
- `convex/settings/mutations.ts` — updateSettings

### 2. Simplify SettingsPage.tsx
- Remove API key fields (keys are now server-side Convex env vars)
- Keep: LLM model selection, web search toggle, rerank top N
- Connect to Convex queries/mutations

### 3. Replace UserMenu with Clerk's UserButton
- Import `<UserButton />` from `@clerk/clerk-react`
- Replace custom avatar + popover with Clerk's built-in component
- Keep theme toggle (add as custom menu item or separate button)

### 4. Add LangSmith tracing
- In Convex actions that call LLMs, add LangSmith Node.js SDK tracing
- Set `LANGSMITH_API_KEY` and `LANGSMITH_PROJECT` in Convex env vars

### 5. Dark mode verification
- Should work as-is (Tailwind CSS class-based)
- Test and fix any issues

## Phase 7: Deploy + Cleanup

### 1. Deploy Convex to production
```bash
npx convex deploy
```
- Set all production env vars in Convex dashboard

### 2. Connect to Vercel
- Connect GitHub repo to Vercel
- Set build command: `cd frontend && npm run build`
- Set output directory: `frontend/dist`
- Set env vars: `VITE_CONVEX_URL`, `VITE_CLERK_PUBLISHABLE_KEY`

### 3. Delete old code
- `/backend/` directory (entire Python backend)
- `/supabase/` directory (migrations, config)
- `/scripts/` directory (PowerShell service scripts)

### 4. Update documentation
- Update `CLAUDE.md` with new dev workflow:
  - `npx convex dev` (start Convex dev server)
  - `npm run dev` (start Vite dev server)
  - No more Python backend or PowerShell scripts
- Update `PROGRESS.md`
- Update `.env.example`

### 5. Final E2E validation
- Sign in with Clerk on production URL
- Upload document → processing completes
- Chat with document → streaming works, tools work
- Settings page works
- Theme toggle works

## Validation
- Full E2E on production URL
- No references to Supabase, FastAPI, or Python anywhere in codebase
- Clean build with no warnings
